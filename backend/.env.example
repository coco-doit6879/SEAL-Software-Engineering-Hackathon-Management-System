PORT=5000
# MongoDB connection string (Note: Prisma requires replica set support for transactions, standard in MongoDB Atlas)
DATABASE_URL="mongodb://localhost:27017/seal_hms?replicaSet=rs0"
JWT_SECRET="your_jwt_super_secret_key_for_seal_hms"
JWT_EXPIRES_IN="7d"

# GitHub Configuration for scraper (Optional)
GITHUB_PERSONAL_ACCESS_TOKEN=""
